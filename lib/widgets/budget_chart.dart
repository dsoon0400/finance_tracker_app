import 'package:flutter/material.dart';

class BudgetChart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Budget Chart Placeholder'),
    );
  }
}
